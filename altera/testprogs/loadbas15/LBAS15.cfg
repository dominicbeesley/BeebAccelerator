MEMORY {
    ZP:  start = $0000, size = $0020, type = rw, define = yes;
    ROM0: start = $3000, size = $300, file = %O, fill=yes;
}
SEGMENTS {
    CODE:     load = ROM0, type = ro;
    ZEROPAGE: load = ZP,  type = zp;
}
FEATURES {
}
SYMBOLS {
}


